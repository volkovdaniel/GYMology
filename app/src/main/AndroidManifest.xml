<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="team13.gymology">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_screen_icon"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_screen_icon_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.GYMology">
        <activity android:name=".ExerciseDetails" />
        <activity android:name=".WorkoutDetails" />
        <activity android:name=".Calendar" />
        <activity android:name=".Profile" />
        <activity android:name="profile.gymology.BMI" />
        <activity android:name=".CreateWorkout" />
        <activity android:name=".Stopwatch" />
        <activity android:name=".Statistics" />
        <activity android:name=".WorkoutDatabase" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>